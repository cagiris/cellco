/**
 * Custom Javascript.
 */
(function () {
	$(".form-date").datetimepicker({
        format: "dd MM yyyy",
        autoclose: true,
        todayBtn: true,
        pickerPosition: "bottom"
    });
} ());